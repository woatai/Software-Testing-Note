# Obsidian local workspace state
.obsidian/workspace.json
.obsidian/workspace-mobile.json

# Keep recordings local unless explicitly added.
*.mp3
*.wav
*.m4a
*.aac
*.flac
*.ogg
*.wma

# Keep resumes local unless explicitly added.
*.pdf
*简历*
*resume*
*Resume*
*RESUME*
*CV*
*cv*

# Keep local SSH keys and command scratch files private.
codex_cloud_ed25519
codex_cloud_ed25519.pub
ssh-command.md
pwsh.txt

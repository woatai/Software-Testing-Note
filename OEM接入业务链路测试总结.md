# OEM 接入业务链路测试总结

## 1. 业务链路理解

这条链路可以理解为：OEM 租户接入后，通过会员中心完成用户注册/映射、权益开通、能力使用、自动扣减和状态查询。

完整闭环是：

```text
获取租户凭据
-> 用户注册/映射影子用户
-> 开通 App / AI / 点点签等权益
-> 用户在 OEM App 或 PDF 端内使用功能
-> 系统根据使用情况自动扣减权益
-> OEM 查询订阅状态、AI 余额、点点签余额
```

核心验证目标不是单接口能否调通，而是：

```text
谁调用
给谁开通
开通了什么
能不能使用
用了扣多少
扣完查得准不准
租户和用户之间会不会串
```

## 2. 影子用户理解

影子用户是会员中心或我方系统内部创建的用户记录，用来承接 OEM 外部用户身份。它不是 OEM App 里的真实账号，而是我方系统识别、授权、扣减、统计时使用的内部用户。

通常通过下面的关系判断：

```text
tenantCode + oemUserId -> internalUserId / shadowUserId / oemUserToken
```

测试时可以结合接口和数据库验证：

- 第一次调用 `/oem/user/register`，应创建内部影子用户和 OEM 用户映射关系。
- 第二次用相同 `tenantCode + oemUserId` 注册，应返回同一个 `oemUserToken`，不能重复创建用户。
- 不同 `tenantCode` 即使使用相同 `oemUserId`，也应该生成不同影子用户。
- 后续权益开通、功能使用、余额扣减，都应落到这个影子用户上。

可以查库辅助确认，例如：

```sql
select *
from oem_user_mapping
where tenant_code = 'tenantA'
  and oem_user_id = 'user_001';
```

关键标准：

```text
同一个 tenantCode + oemUserId，只能对应一个影子用户。
不同 tenantCode 的用户必须隔离。
```

## 3. 商品与订阅测试点

已有商品可以作为 SKU / 套餐测试数据：

| product_id | 商品 | 支付成功后应开通的权益 |
| --- | --- | --- |
| 151 | App 权益年订阅 | PDF/App 功能权限 |
| 153 | AI 权益年订阅 | AI 功能或 AI 额度 |
| 152 | 点点签权益年订阅 | 电子签/签署次数或签署权限 |
| 154 | App + AI + 点点签 年订阅捆绑 | 三类权益都开通 |
| 157 | SR 年订阅 + PDF 7 天赠送捆绑 | SR 年订阅 + PDF/App 7 天赠送权益 |

每个商品建议按这个闭环测试：

```text
注册影子用户
-> 查询初始权益
-> 购买指定 product_id
-> 支付成功或模拟支付成功回调
-> 查询订阅状态和余额
-> 使用对应功能
-> 验证能用的能用，不能用的不能用
-> 验证扣减、到期、取消逻辑
```

重点测试点：

- `151` 买前 PDF/App 功能不可用，买后 PDF 编辑、转换、OCR 等可用；AI 和点点签不应被误开通。
- `153` 买后只开通 AI 权益；App 和点点签不应被误开通。
- `152` 买后只开通点点签权益；App 和 AI 不应被误开通。
- `154` 买后 App、AI、点点签三类权益都应开通。
- `157` 要区分两个有效期：SR 是年订阅，PDF/App 是 7 天赠送；第 8 天 PDF 赠送权益应失效，SR 仍有效。

还要验证重复购买、续期、叠加、覆盖、退款、取消、到期后的规则是否符合产品定义。

## 4. 新模式理解：支付中台与会员中心解耦

新模式下，支付中台只负责收钱和支付结果处理，会员中心负责用户和权益。

可以理解为：

```text
支付中台：创建订单、跳转支付、接收 Paddle/MOR 回调、记录支付状态、通知会员中心。
会员中心：识别用户、处理 product_id 到权益包的映射、开通订阅、增加额度、管理有效期、提供登录/注册接口。
PDF 厂商：调用会员中心登录/注册接口，拿到用户身份后在端内使用能力。
```

“中台不直接写 PDF 权益、不直接加 AI 额度、不直接加点点签次数”的意思是：

```text
中台只告诉会员中心：钱收到了，哪个用户买了哪个商品。
会员中心再决定：给这个用户开通什么权益、加多少额度、有效期多久。
```

例如用户购买 `product_id=154`：

```text
支付中台做：
1. 创建订单
2. 调 Paddle 收款
3. 接收支付成功回调
4. 把订单状态改为 paid
5. 通知会员中心用户购买了 product_id=154

支付中台不做：
1. 不直接开 PDF/App 权益
2. 不直接加 AI 额度
3. 不直接加点点签次数
4. 不直接修改会员权益表、AI 额度表、电子签次数表

会员中心做：
1. 收到支付成功通知
2. 根据 product_id=154 识别权益包
3. 给用户开 App 权益
4. 给用户开 AI 权益或额度
5. 给用户开点点签权益或次数
6. 记录权益开通流水
```

这样设计的好处是职责清楚：订单系统管钱，会员中心管权益，避免两个系统都写权益导致规则不一致。

## 5. 核心测试点

### 5.1 鉴权与安全

所有 OEM 接口调用都应校验 Header，例如：

```text
X-Tenant-Code
X-App-Key
X-Timestamp
X-Signature
```

测试点：

- 缺少任意 Header 应失败。
- `tenantCode` 不存在应失败。
- `appKey` 错误应失败。
- `appSecret` 错误导致签名不匹配应失败。
- 时间戳过期应失败。
- 请求体被篡改但签名不变应失败。
- A 租户不能查询或使用 B 租户用户的权益。
- `oemUserToken` 伪造、为空、过期时不能使用。

### 5.2 用户注册/映射

测试点：

- 新用户注册成功，返回 `oemUserToken`。
- 同一租户同一 `oemUserId` 重复注册，返回同一个影子用户。
- 不同租户使用相同 `oemUserId`，应映射到不同影子用户。
- 缺少 `oemUserId`、邮箱格式非法、名称超长等参数应校验失败。
- 登录/注册接口需要鉴权，PDF 厂商不能越权访问其他渠道用户。

### 5.3 支付与 Paddle/MOR 回调

测试点：

- 创建订单成功，金额、币种、product_id 正确。
- Paddle 支付成功后，中台订单状态变为 paid。
- 支付失败、取消时不能开通权益。
- Paddle 重复回调不能重复开通权益。
- Paddle 回调签名必须校验。
- 金额、币种、product_id 被篡改时不能开通权益。
- 退款、取消订阅、续费、到期回调要正确同步到会员中心。

### 5.4 中台通知会员中心

测试点：

- 支付成功后，中台能通知会员中心。
- 通知内容应包含订单号、用户标识、product_id、支付状态、金额、币种、支付时间。
- 会员中心接口超时或失败时，中台应重试或有补偿机制。
- 同一订单重复通知，会员中心必须幂等处理。
- 用户不存在、商品不存在、权益包不存在时要有明确失败记录或补偿流程。

核心预期：

```text
一笔支付成功订单，最终只开通一次对应权益。
```

### 5.5 会员中心权益处理

测试点：

- 收到 `product_id=151`，只开通 App/PDF 权益。
- 收到 `product_id=153`，只开通 AI 权益或 AI 额度。
- 收到 `product_id=152`，只开通点点签权益。
- 收到 `product_id=154`，同时开通 App、AI、点点签。
- 收到 `product_id=157`，开通 SR 年订阅，并赠送 PDF 7 天。
- 重复收到同一订单，不重复开通、不重复加额度。
- 用户已有权益再次购买时，按规则续期、叠加或覆盖。
- 捆绑商品部分权益开通失败时，应有回滚或补偿机制。
- 到期、取消、退款后权益状态应正确变化。

### 5.6 功能使用与扣减

测试点：

- 未订阅 App 权益时，PDF 编辑、转换、OCR 不可用。
- 订阅 App 权益后，PDF 功能可用。
- 未购买 AI 权益时，AI 摘要、翻译、问答不可用。
- 购买 AI 权益后，AI 功能可用，并按规则扣减额度。
- 未购买点点签时，电子签不可用。
- 购买点点签后，电子签可用，并按规则扣减次数。
- AI 调用失败、电子签发起失败、下游超时等场景，是否扣减必须符合业务规则。
- 同一业务请求重试不能重复扣减。
- 并发请求不能把余额或次数扣成负数。

扣减一致性示例：

```text
AI 初始额度 100
-> 发起一次预计消耗 10 的 AI 请求
-> 请求成功
-> 查询余额应为 90
-> 重复查询结果一致
-> 扣减流水只有一条
-> 重放同一请求不能再次扣 10，除非业务明确允许
```

### 5.7 查询、管理与对账

测试点：

- `GET /oem/subscription/status` 返回订阅状态、有效期、功能列表。
- `GET /oem/ai/quota/balance` 返回 AI 总量、已用、剩余。
- `GET /oem/esign/quota/balance` 返回点点签总量、已用、剩余。
- `POST /oem/subscription/cancel` 取消后，订阅状态和功能可用性应同步变化。
- 支付中台订单 paid 时，会员中心应有 active 权益。
- 支付失败订单不应存在 active 权益。
- Paddle 已退款时，会员中心权益应按规则取消或失效。
- 对账应能发现支付成功但权益未开通、权益已开通但订单未支付等异常。

## 6. P0 优先测试清单

优先保证这些场景：

```text
1. 鉴权签名正确，非法请求被拦截。
2. 用户注册/映射正确，同用户幂等，不同租户隔离。
3. 支付成功后，中台订单状态正确，并通知会员中心。
4. 支付失败/取消后，不开通权益。
5. Paddle 重复回调、中台重复通知，不重复开通权益。
6. product_id 和权益包映射正确。
7. App、AI、点点签单品权益互不串。
8. 154 捆绑商品能开通三类权益。
9. 157 能正确处理 SR 年订阅和 PDF 7 天赠送的不同有效期。
10. 用户登录 PDF 端后，只能使用自己已购买的权益。
11. AI 额度和点点签次数扣减准确，不能超扣或重复扣。
12. 退款、取消、到期后权益状态正确。
```

## 7. 一句话结论

这套新模式的关键是支付和权益解耦：

```text
支付中台只负责“钱收到了没有”；
会员中心负责“给谁开通什么权益”；
PDF 厂商通过会员中心完成登录/注册和权益识别。
```

测试时要重点验证：

```text
钱收到了没有
通知到了没有
用户识别对不对
权益开通对不对
功能能不能用
使用后扣减准不准
取消/退款/到期后状态对不对
```